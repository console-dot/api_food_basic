const { User } = require("./User");
const { PurchaseController } = require("./Purchase");
const { SalesController } = require("./Sale");
const { ExpenseController } = require("./Expense");

module.exports = {
  User,
  PurchaseController,
  SalesController,
  ExpenseController,
};
